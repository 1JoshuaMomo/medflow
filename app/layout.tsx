import type { Metadata, Viewport } from 'next'
import { Inter, Space_Grotesk } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter'
});

const spaceGrotesk = Space_Grotesk({ 
  subsets: ["latin"],
  variable: '--font-space-grotesk'
});

export const metadata: Metadata = {
  title: 'MedFlow AI | Turn Missed Appointments Into Revenue',
  description: 'AI-powered clinic operations platform that predicts no-shows, fills empty slots, and automates your clinic in real time. Increase revenue by 10-30%.',
  generator: 'MedFlow AI',
  keywords: ['clinic management', 'AI healthcare', 'no-show prediction', 'revenue optimization', 'medical scheduling'],
  authors: [{ name: 'MedFlow AI' }],
  openGraph: {
    title: 'MedFlow AI | Turn Missed Appointments Into Revenue',
    description: 'AI-powered clinic operations platform that predicts no-shows, fills empty slots, and automates your clinic in real time.',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport: Viewport = {
  themeColor: '#0a0a0f',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${spaceGrotesk.variable} bg-background`}>
      <body className="font-sans antialiased min-h-screen">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
